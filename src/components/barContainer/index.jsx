import React from 'react';
import SideBarNav from '../sideBarNav';
import menu from '../../data/file';

export default function BarContainer() {
  return( <>
    <SideBarNav data={menu}/>
  
  </>
  );
}
